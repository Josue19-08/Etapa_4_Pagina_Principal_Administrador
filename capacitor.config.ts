import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ease_pagina_administrador',
  webDir: 'www'
};

export default config;
